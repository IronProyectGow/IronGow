<h2 class="ml-5 mt-3">Music Bars</h2>


{{#each bars as |bar|}}
<div class="card m-5 bg-light" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{bar.name}}</h5>
    <a href="/artists/{{bar.id}}" class="card-link">Details</a>
  </div>
</div>
{{/each}}